# Database

PostgreSQL schema, ERD, migrations, and seed data live here (or as Laravel migrations under `backend/laravel-api/database`).

- `schema/` - raw SQL / ERD exports
- `seeders/` - reference seed data
